extends base.pug
include mixins/video.pug

block content 
    form(method="get") 
        input(type="text", placeholder="Search", name="searchKeyword" ,required)
        input(type="submit", value="Search")
    div 
        each video in videos 
            +video(video)