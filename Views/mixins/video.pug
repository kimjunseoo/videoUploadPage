mixin video(video)
    div 
        h4 
            a(href=`/videos/${video._id}`) Video TItle : #{video.title}
        h4 
            span Video description : #{video.description}
        h4 
            span Video created at :#{video.createdAt}
        hr